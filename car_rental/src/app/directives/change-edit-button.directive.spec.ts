import { ChangeEditButtonDirective } from './change-edit-button.directive';

describe('ChangeEditButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeEditButtonDirective();
    expect(directive).toBeTruthy();
  });
});
